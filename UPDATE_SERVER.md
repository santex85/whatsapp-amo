# Инструкция по обновлению на сервере

## Шаги для обновления на сервере

### 1. Подключитесь к серверу

```bash
ssh user@alexhosting.ru
cd /path/to/whatsapp-amo
```

### 2. Сохраните текущие данные

```bash
# Сделайте резервную копию базы данных
cp storage/database/*.db storage/database/backup_$(date +%Y%m%d_%H%M%S).db

# Сохраните .env файл (если он есть изменения)
cp .env .env.backup
```

### 3. Получите обновления из репозитория

```bash
git pull origin main
```

### 4. Установите новые зависимости

```bash
npm install
```

### 5. Проверьте и обновите .env файл

Убедитесь, что в `.env` файле присутствуют все необходимые переменные:

```bash
# Сравните с .env.example
cat .env.example
```

**ВАЖНО: Добавьте следующие переменные для системы авторизации:**

```env
# Авторизация администратора
ADMIN_USERNAME=your_admin_username
ADMIN_PASSWORD=your_secure_password

# Секрет для сессий (обязательно для production)
SESSION_SECRET=your_random_secret_string_min_32_chars
```

**Генерация SESSION_SECRET:**

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 6. Соберите проект

```bash
npm run build
```

### 7. Перезапустите приложение

**Если используете PM2:**

```bash
pm2 restart whatsapp-amo
# Или
pm2 restart all
pm2 logs whatsapp-amo
```

**Если используете systemd:**

```bash
sudo systemctl restart whatsapp-amo
sudo systemctl status whatsapp-amo
sudo journalctl -u whatsapp-amo -f
```

**Если запускаете напрямую:**

```bash
# Остановите текущий процесс (Ctrl+C или kill)
npm start
```

### 8. Проверьте работу

1. Откройте браузер: `https://alexhosting.ru/login`
2. Войдите с учетными данными из `.env` (ADMIN_USERNAME/ADMIN_PASSWORD)
3. Проверьте работу основных страниц:
   - `/` - Dashboard
   - `/accounts` - Список аккаунтов
   - `/health` - Статус системы
   - `/amocrm/connect` - Подключение amoCRM

### 9. Проверьте логи

```bash
# PM2
pm2 logs whatsapp-amo --lines 50

# systemd
sudo journalctl -u whatsapp-amo -n 50 -f

# Напрямую
tail -f logs/app.log
```

## Что нового в этом обновлении

### ✨ Новые возможности:

1. **Система авторизации**
   - Защита всех страниц паролем
   - Страница логина (`/login`)
   - Управление сессиями через `express-session`

2. **HTML страницы для всех endpoints**
   - `/` - Dashboard (главная страница)
   - `/accounts` - Список всех WhatsApp аккаунтов
   - `/accounts/:accountId` - Детальная информация об аккаунте
   - `/amocrm/connect` - Форма подключения amoCRM
   - `/health` - Статус системы
   - `/qr/:accountId` - Страница QR-кода (уже была)

3. **Новые скрипты**
   - `scripts/check-users.js` - Проверка пользователей в БД
   - `scripts/reset-admin.js` - Сброс всех пользователей
   - `scripts/test-login.js` - Тестирование авторизации

### 🔧 Технические изменения:

- Добавлена таблица `users` в БД
- Добавлены зависимости: `express-session`, `bcrypt`
- Обновлена структура маршрутов с поддержкой HTML и JSON
- Добавлен middleware для защиты маршрутов

### ⚠️ Важные замечания:

1. **Обязательно настройте `.env`** перед первым запуском:
   - `ADMIN_USERNAME` - имя администратора
   - `ADMIN_PASSWORD` - пароль администратора
   - `SESSION_SECRET` - секретный ключ для сессий (минимум 32 символа)

2. **Первая инициализация:**
   - При первом запуске система автоматически создаст администратора из `.env`
   - Если пользователи уже есть в БД, администратор НЕ будет создан автоматически

3. **Безопасность:**
   - Не коммитьте `.env` файл в репозиторий
   - Используйте сложный `SESSION_SECRET` в production
   - Все пароли хранятся в виде bcrypt хешей

## Откат к предыдущей версии (если что-то пошло не так)

```bash
# Вернитесь к предыдущему коммиту
git log --oneline -10  # Найдите нужный коммит
git checkout <commit-hash>

# Восстановите зависимости
npm install

# Пересоберите
npm run build

# Перезапустите
pm2 restart whatsapp-amo  # или systemctl restart
```

## Поддержка

Если возникли проблемы:
1. Проверьте логи приложения
2. Убедитесь, что все переменные в `.env` заполнены
3. Проверьте, что порты не заняты другими процессами
4. Убедитесь, что Redis запущен (если используется)
